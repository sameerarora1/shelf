# Optional OpenAI analyzer configuration. The deterministic analyzer is the default
# and does not require any API keys.
SHELF_ANALYZER=deterministic
# OPENAI_API_KEY=sk-...
# SHELF_OPENAI_MODEL=gpt-4.1-mini
